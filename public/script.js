const SERVER_URL_LOCAL = "http://192.168.1.154:8081"; // Reemplazar por la IP local del servidor y el puerto que se haya definido para el servicio HTTP

// Definición de los endpoints de la API REST del servicio
const ENDPOINTS = {
  USERS: "/users",
  LOGIN: "/login",
};

// API del cliente
// Pendiente de definir los endpoints de la API REST del cliente. Se pueden definir tantos endpoints como se necesiten para el cliente, empleando los métodos HTTP adecuados (GET, POST, PUT, DELETE, etc.) y el formato de las rutas que se considere más adecuado para cada caso.
